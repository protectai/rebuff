from setuptools import setup, find_packages

setup(
    name="rebuff",
    version="0.0.2",
    packages=find_packages(),
    install_requires=[
        # Add dependencies here.
    ],
    test_suite="tests",
)
